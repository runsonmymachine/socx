# -----------------------------------------------------------------------------
# General Settings
# -----------------------------------------------------------------------------

# number of spaces per tab
#
INDENT_WIDTH = 4

# tokens for inline comments, escape sequences such as \n, \" \# etc.
# are also supported.
#
COMMENT_TOKENS = '#'

# -----------------------------------------------------------------------------
# Filesystem Settings
# -----------------------------------------------------------------------------

# path to project directory
#
WAREA = /project/users/${USER}/vw_e0

# paths to one or more LST/SystemVerilog input source files.
#   
# paths may be an explicit filetype or a as a glob pattern to search for
# any matching files.
#
INPUT_PATHS = ${WAREA}/PIXIE_ROM_FW/PixieROMApp/TapeOutRelease/PIXIE_E4/PxieParserOut

# possible values: LST / VERILOG / SYSTEM_VERILOG
#
INPUT_FILETYPE = LST

# path to output directory
#
OUTPUT_DIRECTORY = ${WAREA}/vw_e0/pixie_verif/pixie_top

